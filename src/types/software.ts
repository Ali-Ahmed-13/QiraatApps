export interface SoftwareResource {
  id: string; // SEO slug
  name: string; // Arabic title
  version: string;
  size: string;
  category: string;
  description: string; // Detailed Arabic description
  iconUrl: string;
  resourceUrl: string; // External direct download link
}
